/** *  button_suspend.js *  ----- */ig.module(    'game.entities.menus.button_suspend').requires(    'plugins.button.button').defines(function() {    'use strict';    ig.global.EntityButton_suspend = ig.Button.extend({        name: 'btn_suspend',        text: ['Suspend'],        pressedUp: function() {            // Check if browser is capable of using local storage.            if(ig.game.storage.isCapable()) {                // Store global data                ig.game.storage.set('currentLevel', ig.global.currentLevel);                ig.game.storage.set('gold', ig.global.gold);                // Store battle data                ig.game.storage.set('activeUnit', ig.game.activeUnit);                ig.game.storage.set('turn_counter', ig.game.turn_counter);                // Store unit data                ig.game.storage.set('units', ig.game.units);                console.log('Game suspended.');                // Return to start screen                ig.music.fadeOut(2);                ig.system.setGame(ig.StartScreen);            } else {                console.log('Cannot suspend game progress. Your browser is not capable of using local storage.');            }        }    });});