/** *  button_attack.js *  ----- */ig.module(    'game.entities.menus.button_attack').requires(    'plugins.button.button').defines(function() {    "use strict";    ig.global.EntityButton_attack = ig.Button.extend({        name: 'btn_attack',        text: ['Attack'],        pressedUp: function() {            // Override default cursor property to force snap to grid (assist user in selecting tiles)            ig.game.pointer.forceSnapToGrid = true;            // All combat preparations are set; signal main game to wait for user to select target            ig.game.battleState = 'attack';        }    });});