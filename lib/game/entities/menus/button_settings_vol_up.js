/** *  button_vol_up.js *  ----- */ig.module(    'game.entities.menus.button_settings_vol_up').requires(    'plugins.button.button').defines(function() {    'use strict';    ig.global.EntityButton_settings_vol_up = ig.Button.extend({        name: 'btn_settings_vol_up',        text: ['+'],        pressedUp: function() {            // Increase volume            var vol = ig.music.volume + 0.1;            ig.music.volume = vol <= 1 ? vol : 1;        }    });});