/** *  button_inventory.js *  ----- */ig.module(    'game.entities.menus.button_inventory').requires(    'plugins.button.button',    'game.entities.menus.button_inventory_item').defines(function() {    "use strict";    ig.global.EntityButton_inventory = ig.Button.extend({        name: 'btn_inventory',        text: ['Inventory'],        pressedUp: function() {            // Kill all first tier buttons            ig.global.killAllButtons(ig.Button);            // Check if inventory button #0 exists. If not spawn them, else do nothing.            if(typeof ig.game.getEntityByName('btn_item_0') === 'undefined')                for(var i = 0; i < ig.game.units[ig.game.activeUnit].item.length; i++)                    ig.game.spawnEntity(ig.global.EntityButton_inventory_item, ig.game.screen.x + 32, ig.game.screen.y + (i + 1) * 32, {index: i});        }    });});