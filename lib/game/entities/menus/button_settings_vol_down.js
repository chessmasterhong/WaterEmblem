/** *  button_vol_down.js *  ----- */ig.module(    'game.entities.menus.button_settings_vol_down').requires(    'plugins.button.button').defines(function() {    'use strict';    ig.global.EntityButton_settings_vol_down = ig.Button.extend({        name: 'btn_settings_vol_down',        text: ['-'],        type: 'bgm',        pressedUp: function() {            // Decrease volume            if(this.type === 'bgm') {                var vol = ig.music.volume - 0.1;                ig.music.volume = ig.game.auxMusic.volume = vol >= 0 ? vol : 0;            } else if(this.type === 'sfx') {                var vol = ig.game.sfx.volume - 0.1;                ig.game.sfx.volume = vol >= 0 ? vol : 0;            }        }    });});