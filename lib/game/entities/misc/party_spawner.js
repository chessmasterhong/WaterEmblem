/** *  party_spawner.js *  ----- *  Acts as a placeholder for the game to determine where to spawn the party on *  the map. * *  Note: Only put one and only one of this entity on the map. Placing *  additional of this entity may (or may not) result in random party spawn *  locations. */ig.module(    'game.entities.misc.party_spawner').requires(    'impact.entity',    'game.entities.players.hero' // Main character).defines(function() {    'use strict';    ig.global.EntityParty_spawner = ig.Entity.extend({        name: 'party_spawner',        // This entity doesn't have graphics, so we create a box that        // is displayed within Weltmeister to represent this entity.        // Tell Weltmeister how to render the object inside edit view.        // The following draws a green 8x8 resizable box.        _wmBoxColor: 'rgba(0, 255, 255, 0.3)',        _wmDrawBox: true,        _wmScalable: false, // Should not be scalable        size: {x: 32, y: 32}, // Should occupy only one tile        init: function(x, y, settings) {            this.parent(x, y, settings);        },        // Override update method and remove this.parent() so as not to        // waste render cycles drawing an entity with no graphics.        update: function() {}, // End update method    }); // End EntityParty_spawner}); // End .defines